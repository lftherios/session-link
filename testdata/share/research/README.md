# Synthetic research handoff

Atlas and Beacon are fictional products. No claims here come from live research.

Select the comparison passage, include the earlier prompt, and add the author note. The marker `OMITTED_INTERNAL_STRATEGY` is copied into every likely leakage channel: unused output, repeated history, raw payloads, source details, names, metadata, an extension, attachment metadata and unknown message fields. It must be absent from the complete exported document.
