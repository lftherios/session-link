// @session-link/viewer — the React component that renders a session/v0
// document. Single-source between local `slink open` and the hosted page,
// so a preview is byte-for-byte what a recipient sees.
export { RunViewer } from "./RunViewer";
